import React from "react";
import { Outlet, Link } from "react-router-dom";

/*Navigation bar */ 
function Header() {
	return (
		<header>
			<h1>Tamapluggi</h1>

		</header>
	);
}

export default Header;
